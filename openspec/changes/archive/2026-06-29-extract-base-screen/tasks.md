## 1. Create BaseScreen

- [x] 1.1 Create `BaseScreen.java` with camera, viewport, batch, font, app fields and show/dispose lifecycle

## 2. Refactor simple screens

- [x] 2.1 Make `MainMenuScreen` extend `BaseScreen`, remove duplicated boilerplate
- [x] 2.2 Make `GameOverScreen` extend `BaseScreen`, remove duplicated boilerplate
- [x] 2.3 Make `VictoryScreen` extend `BaseScreen`, remove duplicated boilerplate

## 3. Verify

- [x] 3.1 Build project with `gradlew :core:compileJava`
