## 1. Create subpackages and move model classes

- [x] 1.1 Create `data/model/` and move entity classes (MoveEntity, NpcEntity, ProjectileEntity, QuizData, MapData, MapEntry, EnemyEntity)
- [x] 1.2 Update package declarations for all 7 model classes

## 2. Move loader classes

- [x] 2.1 Create `data/loader/` and move loader classes (MapLoader, NpcLoader, QuizLoader, EnemyLoader)
- [x] 2.2 Update package declarations for all 4 loader classes

## 3. Update imports

- [x] 3.1 Update all imports across the project (14 files)

## 4. Verify

- [x] 4.1 Build project with `gradlew :core:compileJava`
